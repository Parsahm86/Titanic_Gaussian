# Titanic_Gaussian
A Titanic survival prediction pipeline using Naive Bayes. Performs EDA, handles missing values, encodes categorical variables, and applies GridSearchCV for hyperparameter tuning. Achieves optimized classification with cross-validation.
